# Friction Wellbeing Agent Notes

- Keep the app native Android with Kotlin and Jetpack Compose.
- Keep data local unless a future task explicitly changes that.
- Do not implement notification reading or launcher mode in the initial MVP.
- Prefer existing dependencies. If a new dependency is needed, ask first.
- Update `README.md` and `docs/` when implementation details change.
